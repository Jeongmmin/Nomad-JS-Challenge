// var a = 5;
// // const b = "2"   // this is not a number
// var b = 2;

// var myName = "jeongmin";

// // 변수는 공백이 있으면 안 된다., js -> 공백필요하면 CamelCase사용 / 파이썬은 snake_case

// console.log(545454545);

// console.log("lalalalalala");

// // 5 -> a로 바꾸어도 결과는 같다.
// // 게으른 개발자가 되자!

// console.log(a + b);
// console.log(a * b);
// console.log(a / b);

// console.log("hello " + myName);

// // const = constant -> 값이 절대 바뀔 수 없다

// myName = "JM";

// console.log("Your new name is " + myName);

// // let const 를 보고 코드 짠 사람의 의사를 파악할 수 있다.
// // 기본은 const, 변경 필요할 때는 let

// // js는 업데이트 혹은 패치가 되었다 (10일 만에 만들었으니까) != 업데이트

// // var는 변하면 안 되는것을 바꿔도 알려주지 않음!! => 위험, 값 보호할 수 없다 => let, const만들어짐

// // always const, sometimes let, never var!!!


// boolean

const amIFat = true;    //"true" X
console.log(amIFat);

const areUFat = null;   //아무것도 없는 상태로 채워진 것

let something;
console.log(something);

// null은 절대 자연적으로 생기지 않는다! 우리가 variable안에 어떤 것이 없다는 것을 확실히 하기 위해 쓰는 것이다.
// null = 없다, undefined = 있는데 안 들어있다.
